namespace TicketProject.Models
{
    public static class Enums
    {
        public enum UserRoles
        {
            Admin,
            User
        }
        public enum TicketType
        {
            VIP,
            Normal
        }
    }
}
